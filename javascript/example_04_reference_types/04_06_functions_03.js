function sayHello(name, lastName) {
  console.log('Hello ' + name + ' ' + lastName);
}

// Call the function
sayHello('Bruno', 'Reboucas');
sayHello('Martin');             // -> Hello Martin undefined
